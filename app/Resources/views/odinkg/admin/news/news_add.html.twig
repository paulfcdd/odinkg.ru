{% extends 'odinkg/admin/base.html.twig' %}

{% block top_block %}
    <h2>Новости</h2>
    <small>Добавить новость</small>
{% endblock %}

{% block custom_css %}

{% endblock %}

{% block content_block %}
    <div class="col-lg-12">
{##}
        <div class="ibox float-e-margins">
            <div class="ibox-title">
                <h5>Добавление новости</h5>
            </div>
            {{ form_start(form) }}
            {{ form_end(form) }}
            {{ form_start(form) }}
            <div class="ibox-content">
                <div class="row">
                    <div class="col-xs-12 col-lg-6">
                        <div class="form-group">
                            {{ form_label(form.title) }}
                            {{ form_widget(form.title, {
                                'attr': {
                                    'class': 'form-control'
                                }
                            }) }}
                        </div>
                        <div class="form-group">
                            {{ form_label(form.content) }}
                            {{ form_widget(form.content, {
                                'attr': {
                                    'class': 'form-control news-editor',
                                    'cols': '3',
                                    'rows': '5'
                                }
                            }) }}
                        </div>
                    </div>
                    <div class="col-xs-12 col-lg-6">
                        <div class="form-group">
                            {{ form_label(form.image) }}
                            {{ form_widget(form.image) }}
                        </div>
                    </div>
                </div>
            </div>
            <div class="ibox-footer">
                <div class="btn-group">
                    <a href="{{ path('admin.news') }}" type="button" class="btn btn-success"><span class="fa fa-chevron-left"></span>&nbsp;Назад</a>
                    <button type="submit" class="btn btn-primary"><span class="fa fa-save"></span>&nbsp;Сохранить</button>
                </div>
                <button type="submit" class="btn btn-primary"><span class="fa fa-save"></span>&nbsp;Сохранить</button>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
